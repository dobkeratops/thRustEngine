use std::cmp::*;
use std::cmp;
use std::f32::*;
use std::cmp::{Ord,PartialOrd,PartialEq};
use std::marker::Sized;

fn sqrt(f:f32)->f32 { f.sqrt()}

pub use std::num;
pub use std::num::*;
pub use std::ops::{Add,Sub,Mul,Div,Rem,Neg,BitOr,BitAnd};
